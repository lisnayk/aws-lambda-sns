{
  "name": "aws-lambda-sns",
  "version": "1.0.0",
  "description": "AWS Lambda sns project",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "serverless deploy",
    "deploy-prod": "serverless deploy --aws-profile lambda",
    "run": "serverless invoke -f main",
    "remove": "serverless remove",
    "remove-dev": "serverless remove --aws-profile lambda"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.3",
    "md5": "^2.3.0",
    "pusher": "^5.1.2",
    "serverless-mysql": "^1.5.4"
  }
}
